name: to_do_leaf
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  card_settings_ui: ^1.2.0
  flutter_local_notifications: ^19.4.0
  # shared_preferences: ^2.2.2
  workmanager: ^0.9.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/refresh.png
    - assets/refresh_outline.png
    - assets/refresh_filled.png

    - assets/refresh_light.png
    - assets/refresh_outline_light.png
    - assets/refresh_filled_light.png
